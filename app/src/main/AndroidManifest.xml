<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.co.android.tictactoe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StartActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".PlayerActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".SelectionActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".SinglePlayerSelectionActivity"
            android:theme="@style/SplashTheme" />
        <activity android:name=".SinglePlayerActivity" />
        <activity android:name=".SinglePlayerGrid5Activity" />
        <activity android:name=".MultiPlayerGrid5Activity" />
        <activity android:name=".MultiPlayerGrid3Activity" />
        <activity
            android:name=".PlaceholderActivity"
            android:theme="@style/SplashTheme"></activity>
        <activity android:name=".CreditsActivity"></activity>
    </application>

</manifest>